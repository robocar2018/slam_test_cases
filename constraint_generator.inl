  template <typename T>
    bool RelativePosCostFunctor::operator()(
        const T* const wheel_speed_scalar_ptr, 
        const T* const gps_heading_offset_ptr,
        T* residual_ptr) const
        {
              // write down the definition of the operator  
        }